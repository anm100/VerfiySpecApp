	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* CLAIM r2 */
;
		;
		;
		;
		
	case 5: /* STATE 11 */
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC vm */
;
		;
		
	case 7: /* STATE 2 */
		;
		now.state = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 9: /* STATE 7 */
		;
		now.state = trpt->bup.oval;
		;
		goto R999;

	case 10: /* STATE 14 */
		;
		p_restor(II);
		;
		;
		goto R999;
	}

